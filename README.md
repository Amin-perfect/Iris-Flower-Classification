# Iris Species Classification
 
A multi-class classification model that identifies an Iris flower's species (Setosa, Versicolor, or Virginica) from four physical measurements built during the CodeAlpha Data Science Internship.
 
## Why this problem matters
This is a classic introductory classification problem, but it tests a core ML skill: separating multiple classes that aren't always linearly distinguishable. It's a good benchmark for understanding how a classification pipeline is built end-to-end, from raw measurements to a predicted label.
 
## Approach
- Explored the dataset using pairplots to visualize how sepal/petal measurements separate the three species
- Built a correlation heatmap to understand which features were most predictive
- Split data into training and test sets
- Trained a **Logistic Regression** classifier on sepal length/width and petal length/width
- Evaluated the model using [METRIC — e.g. accuracy score / confusion matrix, fill in from your notebook]
## Result
[FILL IN: e.g. "The model classified species with XX% accuracy on the test set. The confusion matrix showed [setosa was perfectly separable / versicolor and virginica had slight overlap], consistent with known patterns in this dataset."]
 
 
## Tech Stack
Python, Pandas, NumPy, Matplotlib, Seaborn, Scikit-learn, Jupyter Notebook
 
## Project Structure
```
CodeAlpha_IrisFlowerClassification/
├── dataset/
├── notebook/
├── images/
├── README.md
└── requirements.txt
```
 
## Author
Amin (Mr. PerfecT)  BSCS Student | Aspiring Data Scientist & AI Developer
 
